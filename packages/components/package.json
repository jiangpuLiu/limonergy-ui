{
  "name": "@limonergy-ui/components",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build:dependencies": "pnpm --filter @limonergy-ui/hooks build && pnpm --filter @limonergy-ui/utils build",
    "test": "cross-env NODE_ENV=test pnpm run build:dependencies && vitest --run --coverage",
    "test:watch": "cross-env NODE_ENV=test pnpm run build:dependencies && vitest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
