{
  "name": "@limonergy-ui/components",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_ENV=test pnpm --filter @limonergy-ui/hooks build && pnpm --filter @limonergy-ui/utils build && vitest --run --coverage",
    "test:watch": "cross-env NODE_ENV=test pnpm --filter @limonergy-ui/hooks build && pnpm --filter @limonergy-ui/utils build && vitest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
